# Expanded T48 qualification plan

Planned before dispatch: at most two targeted configured-route diagnostics (one
per Kiro worker/reviewer preference, reusing matching fresh evidence), followed
by one automatic easy label-normalizer attempt. No broad model sweep or repeated
Resume. The existing fixture/acceptance bytes and exact unittest command remain
unchanged. T44 remains deferred; no exporter implementation is attempted here.

Use an isolated profile with explicit included access for exactly
`kiro/claude-sonnet-4.5` and `kiro/claude-haiku-4.5`, plus currently eligible
public-free routes, through OmniRoute. The operator reports OpenRouter's balance
was topped up; that is context, not proof of remaining quota and not paid-model
permission. Keep zero paid spend. Global settings remain untouched.

The new marker probe/cache/trace/identity runtime will be committed and frozen
before Prepare. Retain generated app/fixture/acceptance/configuration hashes.
Use the real app adapter/parser with no diagnostic application fallback; gateway
internal fallback cannot be disabled/observed and will be disclosed as unknown.
Do not claim a qualified pinned-model diagnostic. Automatic execution receives
no injected diagnostic provider selections; fresh successful probe observations
can be reused under the same contract identity.

Expect two short diagnostics plus minutes and perhaps a dozen or more execution
requests, based on earlier same-fixture observations (49–195 seconds and
56,298–111,141 accounted tokens for pinned runs). These are expectations, not
caps or guarantees. Measurement mode retains monetary/access/command constraints,
transport safeguards, Pause and usage accounting. Capture separate diagnostic
and execution usage, all naturally occurring failures, and zero or actual
interventions. No account exhaustion exercise or ordinary regression inference.

Inspect the actual candidate, supplied tests, README example, current checks and
completed receipts. Preserve source main and acceptance bytes. No merge/push.
A failed attempt remains a result; propose any further trial separately.
